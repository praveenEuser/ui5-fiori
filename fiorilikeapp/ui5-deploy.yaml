# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "4.0"
metadata:
  name: sasu
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: A4H
          url: http://nr-s4hana2023.narasimhaclouds.com:8023/
          client: '100'
        app:
          name: ZZ1_DEC2
          description: My First Fiori App in SAP Server
          package: ZTEST
          transport: A4HK902178
        exclude:
          - /test/
